{
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:places": "npm run build && node dist/places/build-places-from-db.js"
  },
  "dependencies": {
    "tz-lookup": "^6.1.25"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^20.0.0"
  }
}
