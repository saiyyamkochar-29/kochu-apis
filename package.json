{
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:location": "npm run build && node dist/location/build-location.js",
    "build:age": "npm run build && node dist/age/build-age.js"
  },
  "dependencies": {
    "tz-lookup": "^6.1.25"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^20.0.0"
  }
}
