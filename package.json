{
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:places": "npm run build && node dist/places/build-places-from-db.js"
  },
  "dependencies": {
    "tz-lookup": "^6.1.25",
    "xbox-webapi": "^1.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  }
}
